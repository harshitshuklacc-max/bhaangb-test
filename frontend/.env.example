# Leave empty on Vercel — API uses same domain at /api
# NEXT_PUBLIC_API_URL=

# Local dev (Next.js serves API via pages/api)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Database (required on Vercel — use Vercel Postgres or Neon)
DATABASE_URL="postgresql://user:password@host:5432/smart_step_academy?sslmode=require"

JWT_SECRET="long-random-secret"
JWT_REFRESH_SECRET="long-random-refresh-secret"
ADMIN_USERNAME=smartstep05618
ADMIN_PASSWORD=Smartedhub123
